#autoload

emulate -L zsh

__zest_tty_snapshot || return 1

if [[ $zest_tty_cursor == <-> && $1 == <-> ]]; then
  (( zest_tty_cursor != $1 )) && return 0
elif [[ $zest_tty_cursor != "$1" ]]; then
  return 0
fi
__zest_record_failure "expected CURSOR to not be at ${(qqq)1} but it was"
return 1
