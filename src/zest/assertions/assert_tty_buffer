#autoload

emulate -L zsh

__zest_tty_snapshot || return 1

[[ $zest_tty_buffer == "$1" ]] && return 0
__zest_record_failure "expected BUFFER to be ${(qqq)1} but was ${(qqq)zest_tty_buffer}"
return 1
