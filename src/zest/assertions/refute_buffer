#autoload

emulate -L zsh

local unexpected=$1

__zest_tty_refresh || return 1

if [[ $zest_tty_buffer != $unexpected ]]; then
  return 0
fi

__zest_record_failure "expected BUFFER to not be ${(qqq)unexpected} but was"
return 1
