#autoload

emulate -L zsh

local unwanted=${(V)1}
__zest_tty_snapshot || return 1

[[ $zest_tty_buffer != $unwanted ]] && return 0
__zest_record_failure "expected BUFFER to not be ${(qqq)1} but was"
return 1
