#autoload

emulate -L zsh

local subject=${1:?}
local subject_type

if ! (( $+parameters[$subject] )); then
  return 1
fi

subject_type=${parameters[$subject]}

if [[ $subject_type =~ array ]]; then
  return 0
fi

return 1
