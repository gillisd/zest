#autoload

emulate -L zsh

if [[ -z $__zest_tty_session ]]; then
  __zest_record_failure "tty_type: no live session -- call tty_start first"
  return 1
fi

zpty -w -n $__zest_tty_session "$*" 2>/dev/null
return 0
