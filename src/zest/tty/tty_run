#autoload

emulate -L zsh

if [[ -z $__zest_tty_session ]]; then
  __zest_record_failure "tty_run: no live session -- call tty_start first"
  return 1
fi

if ! __zest_tty_send_synced "$1"; then
  __zest_record_failure "tty_run: command did not complete: ${(qqq)1}"
  return 1
fi
return 0
